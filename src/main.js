// Temporary main.js for vite build compatibility
console.log('Schwepe multi-site project - temp main.js');

const app = document.getElementById('app');
if (app) {
    app.innerHTML += '<p>Build system initialized.</p>';
}