[phases.setup]
nixPkgs = ["nodejs_20", "git"]

[variables]
CI = "true"
NPM_CONFIG_UPDATE_NOTIFIER = "false"
NODE_ENV = "production"
PORT = "3000"

[phases.install]
cmds = [
  "rm -rf node_modules package-lock.json",
  "npm install --no-cache --prefer-offline --omit=dev"
]

[phases.build]
cmds = [
  "npm run build"
]

[start]
cmd = "node server.js"
runPhases = ["install", "build", "start"]

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
port = 3000

# Fixed deployment permissions - 2025-10-27T10:20:15.142Z
# Updated healthcheck path to match actual API endpoint
