{
  "name": "schwepe",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "rebuild-videos": "node scripts/rebuild-video-list.js",
    "rebuild-images": "node scripts/rebuild-image-list.cjs",
    "rebuild-media": "npm run rebuild-videos && npm run rebuild-images",
    "build": "npm run rebuild-media && node build-multi-site.js",
    "build:multi-site": "npm run rebuild-media && node build-multi-site.js",
    "start": "NODE_ENV=production node server.cjs",
    "start:dev": "NODE_ENV=development node server.cjs",
    "test": "node evals/eval.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "type": "module",
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "playwright": "^1.57.0"
  }
}
