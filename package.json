{
  "name": "schwepe",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "rebuild-videos": "node scripts/rebuild-video-list.js",
    "build": "npm run rebuild-videos && node build-multi-site.js",
    "build:multi-site": "npm run rebuild-videos && node build-multi-site.js",
    "start": "NODE_ENV=production node server.cjs",
    "start:dev": "NODE_ENV=development node server.cjs"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "type": "module"
}
